void solve()
{
    int n,x,index_negative=0,index_positive=0;
    cin>>n;
    int negative; // negative : so am
    int positive; // positive: so duong
    int count_zero=0;
    for(int i=0;i<n;i++)
    {
        cin>>x;
        if(x<0)
            negative++;
        else if(x>0)
            positive++;
        else
            count_zero++;
    }
    if(negative%2!=0 || count_zero!=0)
        cout<<0<<'\n';
    else
    {
        cout<<1<<'\n';
        cout<<1<<" "<<0<<'\n';
    }
    A - Least Product ( true)
}
int main()
{
	fast;
	int t;
	cin>>t;
	while(t--)
	{
		solve();
	}
	return 0;
}

